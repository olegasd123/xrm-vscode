Initial scaffold for XRM VS Code extension created. Next up:

- [done] Wire real CRM publish logic inside `src/services/publisherService.ts`.
- [done] Refine context menu to offer separate add vs. publish actions for resources.
- [done] Support publishing all supported files when a bound folder is selected.
- Add tests/linting once dependencies are installed.
